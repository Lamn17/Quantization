# Compatibility export for non-uv environments. pyproject.toml + uv.lock are
# the canonical dependency source for this repository.
torch>=2.1
torchvision>=0.16
ultralytics>=8.3
pycocotools>=2.0.7
numpy>=1.24
pandas>=2.0
PyYAML>=6.0
matplotlib>=3.7
tqdm>=4.66
Pillow>=9.5
# TensorRT is optional and must match the target CUDA/driver environment.
